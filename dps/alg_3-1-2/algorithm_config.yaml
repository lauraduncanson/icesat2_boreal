# DO NOT DELETE
# THIS CONFIG IS AUTO-GENERATED BY ADE UI
algo_name: do_HLS_stack_3-1-2
version: HLS_stack
environment: ubuntu
repository_url: https://repo.ops.maap-project.org/icesat2_boreal/icesat2_boreal.git
#docker_url: mas.maap-project.org:5000/root/ade-base-images/vanilla:v2.1
#docker_url: mas.maap-project.org:5000/root/ade-base-images/r:latest
docker_url: mas.dit.maap-project.org/root/maap-workspaces/base_images/vanilla:dit
# queue chosen when registering
queue: maap-dps-worker-16gb

# fill out these fields
# explain what this algorithm does
description: DPS run to create tiled HLS composites
# path to the build script for installing and building the algorithm in the docker image
build_command: icesat2_boreal/dps/build_command_main.sh

# path to the wrapper script for running the algorithm
run_command: icesat2_boreal/dps/alg_3-1-2/run.sh
# set a storage value in GB or MB or KB, e.g. "100GB", "20MB", "10KB"
disk_space: 16GB

## Include all dynamic inputs that are not hard coded in run.sh
# Every param that is a 'download: False' will be a positional arg in run.sh for the python script
# If 'download: True' then it isnt a positional arg
inputs:
# remove below this line if no inputs
# rename and set algorithm input names accordingly
  - name: in_tile_fn
    download: True
  - name: in_tile_num
    download: False
  - name: in_tile_layer
    download: False
  - name: sat_api
    download: False
  - name: tile_buffer_m
    download: False
  - name: start_year
    download: False
  - name: end_year
    download: False
  - name: start_month_day
    download: False
  - name: end_month_day
    download: False
  - name: max_cloud
    download: False
  - name: composite_type
    download: False